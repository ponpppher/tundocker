<%= link_to image_tag('logo.png', :size => '70x70'), root_path, { class: "nav-brand" } %>
<button class="navbar-toggler" type="button" data-toggle="collapse data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
  <span class="navbar-toggler-icon">
</button>
<div class="collapse navbar-collapse" id="navbarNavDropdown">
  <ul class="navbar-nav mr-auto">
    <li class="nav-item">
      <%= link_to "about", root_path(anchor: :about), { class: "nav-link" } %>
    </li>
    <li class="nav-item">
      <%= link_to "scene", root_path(anchor: :scene), { class: "nav-link" } %>
    </li>
  </ul>

  <ul class="navbar-nav">
    <li class="nav-item">
      <%= link_to icon('fas', 'home'), books_path, { class: 'nav-link' } %>
    </li>

    <% if current_user.name.blank? %>
      <li class="nav-item">
        <strong><%= link_to "No Name User", user_path(current_user.id), { class: 'nav-link' }%></strong>
      </li>
    <% else %>
      <li class="nav-item">
        <strong><%= link_to current_user.name, user_path(current_user.id), { class: 'nav-link' } %></strong>
      </li>
    <% end %>

    <li class="nav-item">
      <%= link_to 'book group', recommends_path, { class: 'nav-link' } %>
    </li>

    <li class="nav-item">
      <%= link_to 'ログアウト', destroy_user_session_path, { method: :delete, class: 'nav-link' } %>
    </li>
